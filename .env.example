# Copy this file to .env and update values for your environment

# REQUIRED: Generate a secure random key for production
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Printer settings
PRINTER_HOST=172.29.208.1
PRINTER_PORT=9100

# Rate limiting (seconds between messages per IP)
RATE_LIMIT_SECONDS=60

# Delay between print jobs (seconds)
PRINT_DELAY_SECONDS=5

# Set to 'true' only for development
FLASK_DEBUG=false
